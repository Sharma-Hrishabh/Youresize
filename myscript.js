const player=document.querySelector('.html5-video-player');
var modWidth = "" + window.innerWidth*0.98 + "px";
var modHeight = "" + window.innerHeight*0.92 + "px";
player.style.width= modWidth;
player.style.height= modHeight;
console.log('done');
